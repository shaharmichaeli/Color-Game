{
  "name": "color-game",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "build": "npm install",
    "heroku-postbuild": "npm install"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
